int minValue(struct Node *root) {
    // your code here
    if(root == NULL){
        return -1;
    }
    while(root->left != NULL){
        root = root->left;
        // res = 
    }
    
    return root->data;
}
